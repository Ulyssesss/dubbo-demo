## 端口号
server.port=8082

## spring 应用名
spring.application.name=consumer-service

## dubbo 应用名，默认跟 spring.application.name 值相同
# dubbo.application.name=${spring.application.name}

## dubbo 注册地址
dubbo.registry.address=zookeeper://127.0.0.1:2181